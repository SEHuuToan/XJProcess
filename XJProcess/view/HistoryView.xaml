<Window x:Class="XJProcess.view.HistoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:XJProcess.view"
        mc:Ignorable="d" 
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Title="HistoryView"
        WindowState="Maximized" d:DesignWidth="2100.257" d:DesignHeight="783.386">

    <!-- Khung nền tổng thể giống SettingView -->
    <Border Background="#F8FAFC" CornerRadius="8" BorderBrush="#0284C7" BorderThickness="2" Margin="10">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Thanh tìm kiếm / Lọc -->
                <RowDefinition Height="27*"/>
                <!-- Nội dung bảng lịch sử -->
                <RowDefinition Height="128*"/>
                <!-- Nút điều hướng / Đóng -->
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TIÊU ĐỀ TRANG -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="8" Height="26" Background="#0284C7" Margin="0,0,10,0"/>
                    <TextBlock Text="LỊCH SỬ VẬN HÀNH &amp; SỰ KIỆN HỆ THỐNG" Foreground="#0F172A" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Grid>

            <!-- KHUNG LỌC DỮ LIỆU - NẰM NGANG THẲNG TẮP -->
            <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Từ ngày -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Từ ngày" Style="{StaticResource FieldLabelStyle}"/>
                        <DatePicker x:Name="DpFromDate" Width="320" Height="40"/>
                    </StackPanel>

                    <!-- Đến ngày -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Đến ngày" Style="{StaticResource FieldLabelStyle}"/>
                        <DatePicker x:Name="DpToDate" Width="320" Height="40"/>
                    </StackPanel>

                    <!-- Mã bồn -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Mã bồn" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox x:Name="TxtFilterDrumId" Width="300" Height="40" Style="{StaticResource HmiInputStyle}" Text="7"/>
                    </StackPanel>

                    <!-- Mã bồn -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Nhân viên thực hiện" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox x:Name="TxtFilterEmp" Width="300" Height="40" Style="{StaticResource HmiInputStyle}" Text="2000000"/>
                    </StackPanel>

                    <!-- Mã bồn -->
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Trạng Thái" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox x:Name="TxtStatus" Width="300" Height="40" Style="{StaticResource HmiInputStyle}" Text="Hoàn Thành"/>
                    </StackPanel>

                    <!-- Nút Tìm kiếm căn chỉnh ngay ngắn bên cạnh -->
                    <StackPanel VerticalAlignment="Bottom">
                        <Button Content="Tìm kiếm" Width="140" Height="40" 
                                Background="#0284C7" BorderBrush="#38BDF8" Foreground="#FFFFFF" 
                                FontSize="16" FontWeight="SemiBold"
                                Style="{StaticResource OverViewButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- BẢNG HIỂN THỊ DỮ LIỆU (DATAGRID) -->
            <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="10">
                <DataGrid x:Name="DgHistory" AutoGenerateColumns="False" CanUserAddRows="False"
                          Background="Transparent" BorderBrush="#E2E8F0" RowHeight="35"
                          ColumnHeaderHeight="40" FontSize="14" FontWeight="Medium"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Thời gian" Binding="{Binding Timestamp}" Width="180"/>
                        <DataGridTextColumn Header="Mã Bồn" Binding="{Binding DrumId}" Width="100"/>
                        <DataGridTextColumn Header="Sự kiện / Thao tác" Binding="{Binding ActionName}" Width="*"/>
                        <DataGridTextColumn Header="Tốc độ (RPM)" Binding="{Binding Speed}" Width="120"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="150"/>
                        <DataGridTextColumn Header="Người thực hiện" Binding="{Binding User}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- NÚT ĐÓNG -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Đóng" Width="120" Height="42" 
                        Background="#C62828" BorderBrush="#94A3B8" Foreground="#FFFFFF" FontSize="14" FontWeight="SemiBold"
                        Style="{StaticResource OverViewButtonStyle}" Click="BtnClose_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>